# Core dependencies — required for the app to run.
# Optional add-ons (voice transcription, body language, stronger semantic
# scoring) live in requirements-optional.txt — see README "Optional setup".

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
sqlalchemy==2.0.35
pydantic[email]==2.9.2
bcrypt==4.2.0
python-jose[cryptography]==3.3.0
pymupdf==1.24.10
requests==2.32.3
scikit-learn==1.5.2
reportlab==4.2.5

